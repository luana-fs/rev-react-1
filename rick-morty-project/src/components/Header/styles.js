 import styled from 'styled-components'

 export const HeaderContainer = styled.header`
    height: 100px;
    background-color: var(--deep-blue-secondary);
    text-align: center;
    font-size: 24px;

    //tem que ser a mesma largura do container
    line-height: 100px;
 `